# Daily Report 2026-06-16

## このコンペは何か（確定事項）

- Pokémon TCG AI Battle Challenge。**Simulation（賞金なし・エージェント提出）** と **Strategy（賞金 $240,000・レポート提出）** の2本立て。
- Strategy の審査は「①エージェント安定性 ②デッキ設計 ③Simulation 成績」→ **強いエージェント＋デッキの構築が賞金の前提**。両方を一体で進める方針。
- 提出物（Simulation）: `main.py`（`agent(obs_dict)->list[int]`）+ `deck.csv`(60行) + `cg/` パッケージ。
- 1試合最大10分・タイムアウト敗北 → 1手の探索時間に予算制約。

## 今日やったこと

- 両コンペのデータ取得（`data/raw/` にカード CSV、`data/sim_sample/` にサンプル提出一式）。
- エンジン (`libcg.so` / ctypes) のインターフェースを解析。観測・行動・**前方探索 API**（`search_begin/step/end`）を把握。
- **ローカルでエンジンが動くことを検証**: ランダム agent 同士の自己対戦が完走（34手, player1 勝利）。`all_card_data()`=1267件取得。
- `competition/overview.md` を実情報で更新。

## 重要な技術的発見

- エンジンに **determinization 探索 API** がある。相手の隠し情報（手札/山札/サイド/伏せアクティブ）を「予測 Card ID」で与えて先読みできる → MCTS / ルックアヘッド系エージェントの土台になる。
- 部分観測（相手 hand=None, deck は枚数のみ）。本番は隠し情報をサンプリングして探索する必要がある。

## 未確定・要確認（実装前ガード）

- **Kaggle Rules タブ未取得**: external data / pretrained models / internet の3項目が TBD。提出コードを書く前に確認する。
- エージェント実行環境のリソース・時間制限（CPU/GPU/メモリ、1手の制限時間）。
- 使用可能カードプール（Card ID List PDF）の正確な範囲。

## 次アクション

1. Kaggle Rules タブを確認し overview.md の Rules を埋める。
2. ローカル対戦ハーネスを `.py` 化（任意の2 agent を対戦させ、勝率・サイド差・手数を集計）。`workspace/exp001_harness/` 。
3. カードデータを整形し、使用可能カードプールと既存メタ（公開デッキ）を把握。
4. ベースライン agent を段階的に: ①合法手の中で意味のある手を打つルールベース → ②1手読み（攻撃価値最大化） → ③探索 API での先読み。
5. 公開 notebook（例: BattleGenesis）/discussion を `references/knowledge/` に要約。

## 追記（Kaggle ページ md を取得・整理）

- `references/raw/kaggle_pages/` に Simulation/Strategy の overview/data/rules を保存。`*:Zone.Identifier` は全削除。
- **Rules 解明**: external data 許可 / pretrained 許可（勝者は MIT 公開義務）/ Pokémon要素で学習したモデルはコンペ外利用禁止 / 5提出/日 / private共有はチーム内のみ。
- **Simulation エンジン = cabt Engine**（kaggle-environments 1.14.10）。API docs https://matsuoinstitute.github.io/cabt/ 。提出は `.tar.gz`（main.py トップレベル + deck.csv）。
- **レーティング = TrueSkill 系** N(μ,σ²), μ0=600, 勝敗のみ反映, 最新2提出で最終評価。エピソードリプレイDL可（BC/RL用）, 上位エピソードの日次エクスポート提供予定。
- **Strategy 評価ルーブリック判明（最重要）**: Model 70%（明快さ/独創性/**安定性**/**頑健性(運非依存)**/LB成績）, Deck 20%, Report 10%。Writeup ≤2000語＋図表。**LB上位は必須でない**→ 安定・筋の通った agent+デッキ+明快なレポートが賞金($30k×8)への道。
- 公式ルールとシミュレータの差分: Simulation discussion 708586（未取得・要確認）。

## メモ

- リポジトリはまだ 1 commit もなし。`.gitignore` 整備＋初回コミットをユーザー承認後に提案する。
