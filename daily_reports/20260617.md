# Daily Report 2026-06-17

## 今日やったこと

- 公開ノートブック2本を取得・整理（`references/raw/public_notebooks/`）し要約（`notebooks.md`）。
  - Beginner Guide（LB 770.4, 公式Lucarioそのまま）→ 提出パッケージング手順を把握。
  - Validated Rule-Based V2（LB 796.4）→ ロジック改良版＋検証済みマッチアップ主張。
- README をコンペ用に全面書き換え、初回コミット push（e853360）。
- **exp002_baselines**: 公式4種＋V2＋random をローカルハーネスに載せ、総当たりマッチアップ表を作成。

## exp002 結果（n=60/pair, swap, seed=42）

強さ（対プール平均勝率）: **lucario_v2 0.680** > lucario_v1 0.647 > dragapult 0.573 > abomasnow 0.543 > iono 0.520 > random 0.037。

重要な発見:
- **LB 770→796(V2)の正体 = 「弱い相手への取りこぼし削減＝安定性」**。V2 は vs random 1.000、V1 は 0.867（V1は時々setup自滅）。V2 vs V1 直接対決は 0.483 でほぼ五分。
- **公開ノートブックの勝率主張は再現せず**。V2 vs Dragapult: 主張0.91 → 実測0.55。V2 vs V1: 主張0.707 → 実測0.58。vs random のみ一致(0.97/0.99)。作者の埋め込み表はハードコードで公開コードでは未生成。
- **このエンジンの先攻有利はほぼ無い**（ミラー先攻勝率0.517）。
- マッチアップはじゃんけん的（iono>abomasnow>... , lucario>iono）。

## 判断・方針への反映

- ローカル強さ指標 = 固定プール平均勝率。**越えるべきバー = lucario_v2 0.680**。
- 安定性/頑健性は LB にも Strategy 評価(Model 70%)にも効く → エージェント設計の主目標に据える。
- 公開数値は鵜呑みにせず、CV/LB 校正は自前ハーネスの数値で行う。

## exp003_lookahead 結果（Search API 先読み agent）— NEGATIVE

- Search API は実機検証で完全に機能（`spike_search.py`）。`searchId` は SearchState 側。
- 自前先読み agent 2モードを実装・評価（deck=lucario_v2, n=30/相手, swap）:
  - one_ply: 対ルールベース平均 **0.211** / turn_rollout: **0.267**。バー 0.680 に遠く及ばず。
  - random 相手でも 0.80–0.87（時々自滅）、lucario_v2/dragapult には完敗。
- 診断: ①評価関数 V() が粗い（エネ/打点/KO閾値/相性を見ない）②相手モデル無し（応手・反撃を無視）③ターン内多段選択の計画性無し。
- 学び: **手製ヒューリスティック先読みでは強ルールベースを超えられない**。Search 統合自体は exp004 の資産。

## exp004_mcts 結果（AlphaZero 自己対戦, GPU）— NEGATIVE（デモ規模）

- GPU 空き確認(11.6GB)→ torch 2.6.0+cu124 導入 → 公式 RL/MCTS サンプルを `train_mcts.py` に移植。
- pipeline は GPU で完走（自己対戦→学習→評価）。run1: 5世代/search16/自己対戦40。
- 学習ログ: loss 0.28→0.036 と減少も、対 random 勝率は振動（75→35→60→65→40%, n=20ノイズ）。
- **exp002 プール評価: gen0(未学習) 0.033 / gen3 0.017**（バー 0.680）。Dragapult/Lucario に全敗。
- 診断: ①データ/計算量が桁違いに不足 ②determinization が相手を placeholder で穴埋め＝偽の相手に planning ③cold-start の立ち上がり遅い。
- 推論コスト 0.3–0.9s/game, maxmove ~1.8s（10分制限に余裕）。

## 今日の総括（exp001→004）

- 強さ序列（プール平均）: ルールベース lucario_v2 **0.680** ≫ ナイーブ先読み(exp003) 0.21–0.27 ≫ デモ規模AlphaZero(exp004) ~0.03。
- **「公式サンプルをそのまま回す」だけでは勝てない**。素朴な探索/学習は、ルールベースが内包するドメイン知識に劣る。
- 方針転換: **BC/IL で warm-start が本命**。強ルールベース(lucario_v2)や公式上位エピソードexportを教師に value/policy を事前学習→RL/MCTS 微調整。cold-start より遥かに効率的。

## 次アクション

1. **exp005(BC/IL)**: lucario_v2 等の対戦ログを大量生成→(obs→選択)を教師に policy/value 学習→agent化してプール評価。
2. BC-net を初期重みに exp004 の RL/MCTS を warm-start 再開。
3. 並行: 提出パイプライン整備＋ lucario_v2 を実提出して LB 校正点を取得。
4. Strategy 観点: まず堅実な提出ベースライン(強ルールベース/BC)を確保し、独自性(RL/MCTS改善)をレポートに載せる。

## メモ
- exp002 の `policies/` は抽出競技コードのため gitignore（`extract_policies.py` で再生成）。
- **torch は `uv pip install` で venv に導入済みだが pyproject 未登録**（CUDA index 指定が必要なため）。正式な依存化はBC路線確定後に検討。
- exp004 の models(.pth ~50MB)/log は results/(gitignore)。
