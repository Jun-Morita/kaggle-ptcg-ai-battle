# 2026-08-07

## 要約

- **探索レーンを閉じた。** A/B を2回（閾値 0.02 / 0.12）とも負の側。価値モデルが同一ターン内の
  兄弟局面を区別できていない。誠実なネガティブとして記録。
- **容量が制約だった。** 誤り診断からの特徴量追加を2周やったあと、ハイパーパラメータを一度も
  動かしていなかった。葉 63 → 511 で main の教師再現が +0.018。
- **v4b（容量のみ変更）が全指標で最良。** 教師再現 0.7616、lucario_v2 0.680（v4 0.575）。
- **検証が素通りしていた不具合を1件修正。** `parity_pure.py` が失敗しても run スクリプトが
  止まらず、**パリティ未検証のまま tar がビルドされていた**。
- ラダーが全体的に下がっている: v051 870.7 → 835.1、v052 847.3 → 815.5。

## 1. 探索の A/B — 採用せず

`search.py`（検証付き上書き）を実装し、価値モデル（80万行、AUC 0.9086）と組み合わせて測定。

```
閾値 0.02   n=60    探索なし 0.567 / 探索あり 0.467   -0.100 (z -1.10)  上書き 10.8%
閾値 0.12   n=120   探索なし 0.608 / 探索あり 0.542   -0.067 (z -1.03)  上書き  5.1%
```

**上書き率を半減させても改善しない**＝上書きが多すぎるのではなく、上書きの選び方が良くない。

原因は価値モデルと考える。AUC 0.9086 は全局面での数字で、終盤が簡単なだけ。探索が必要と
するのは**同じターン内の兄弟局面の順序**で、そこでは差が小さくノイズに埋もれる。学習分布の
偏り（負けの行がミラー戦に偏る、互角局面も勝ちラベル）もこの用途には効いている。

正しく直すには価値の学習方法を変える必要がある（局面の優劣を直接学ぶ、自己対戦で作る）。
締切まで9日でそこに賭けるのは合わないと判断し、レーンを閉じる。

### 途中で出した符号バグ（記録）

最初の測定は **0.015（3-197）** という壊滅的な数字だった。原因は `search.py` に自分で
書いたコメントの通り:

```python
# the evaluated position is scored from the seat to move; ...
# so flip when the position is the opponent's to play
return v      # ← 反転を書いていない
```

`_greedy_finish` は自ターン終了時点で止まるので評価対象は**相手の手番**。`base_state` は
手番側視点なので、探索は**相手の勝率を最大化する手**を選んでいた。今日4件目の
「静かに劣化する不具合」だが、前3件と違い**大きく壊れたので即座に分かった**。
危ないのはもっともらしい数字を出す壊れ方の方。

## 2. 容量スイープ — 見落としていた素直なレバー

main（全決定の45%、誤りの55%）だけを対象に num_leaves を振った。

```
葉   63  lr 0.080   topk 0.6962   ← v4 の設定
葉  127  lr 0.060   topk 0.7071
葉  255  lr 0.050   topk 0.7096
葉  511  lr 0.040   topk 0.7143
葉 1023  lr 0.035   topk 0.7128
葉 2047  lr 0.030   topk 0.7152
```

511 で頭打ち（以降の差 ±0.002 は誤差）。2047 はノード数が3倍になる割に得るものがないので
**511 を採用**。28万クエリに対して葉63枚は単純に手狭だった。

**特徴量を2周いじる前に、これを試すべきだった。**

## 3. v4b — コーパスも特徴量も v4 と同一、容量だけ変更

```
教師再現 (held-out)      v3       v4      v4b
  main                 0.6976  0.6962  0.7114
  c7 (TO_HAND)         0.6154  0.6374  0.6437
  mid                  0.8217  0.8528  0.8575
  low                  0.7569  0.7923  0.7908
  easy                 0.9163  0.9231  0.9245
  全体 exact-set top-k 0.7382  0.7522  0.7616

出荷tarでのゲート (n=200)
  lucario_v2           0.640   0.575   0.680
  Crustle              0.615   0.710   0.705
  dragapult            0.695   0.810   0.755
  Archaludon           0.630   0.685   0.655
  対現行ビルド         0.670   0.650   測定中
```

v4 で下がっていた lucario_v2 が 0.575 → 0.680 に戻り、他は同水準。**v4b が最良**。

モデルは 2.99MB → 16.69MB、tar は 1.5MB → 6.5MB に増えたが、最大手番は 0.23〜0.38秒で
v4（0.43〜0.55秒）より**むしろ速い**（木の本数が減ったため）。

## 4. 【重要】検証が素通りしていた

`run_v4b.sh` の実行中、`parity_pure.py` が `FileNotFoundError` で落ちたのに
**スクリプトが止まらず、パリティ未検証のまま tar がビルドされていた**。

原因は `parity_pure.py` がモデルのタグで行データを探す作りだったこと。v4b は v4 の行で
学習しているので `rows_v4b.pkl` は存在しない。

修正: `--rows` を分離し、**比較件数が 0 なら例外を投げる**ようにした。ファイルが無いことが
「検証OK」に化けない。修正後の v4b は差 0.000・600/600 で通過。

これで**出荷検証が黙って飛ぶ経路**が1つ塞がった。

## 5. ラダー: 全体的な下降

```
          08-06        08-07
v051      870.7   →    835.1
v052      847.3   →    815.5
```

両方が 30点前後下がっている。我々のエージェントが弱くなったのではなく、**参加者の増加と
上位層の強化でラダー全体がデフレしている**と見るのが自然（6,309 → 6,419 人）。

**v052 は v051 を下回ったまま**で、ローカルの優位（対現行 0.670）はラダーで確認できていない。
これで5回目の「ローカルは転移しない」。ただし今回は測り方の条件が過去と違う
（出荷tar直接・現行ビルド相手・全マッチ勝ち越し）ので、単純に同列には置けない。

## 次アクション

1. v4b の対現行ゲート完了を待つ。
2. v052 の収束を見て、v4b の提出可否を判断する。次の提出は **v051 を押し出す**。
3. 残る特徴量の課題: Munkidori（ダメカン移動の特性）を過剰に使う偏り 3,137件が、
   ターン履歴を足しても解消していない。特性の効果が盤面をどう変えるかが見えていないため。
   技のダメージ値と違い engine API から機械的に取れず、手作業になる。
