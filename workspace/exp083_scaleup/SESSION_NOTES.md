# exp083 — スケールアップ BC: 「7分・極小モデル」という投資不足を潰す

## 仮説
exp080 の BC ラダー失敗（0.43）は**容量/表現の壁**と結論していたが、その結論は
**d_model=128 / 1層+1層 / 2ヘッド を合計7分（147秒×3epoch）学習しただけ**の実行から導かれていた。
`pre_grimm10/history.json` の val top-1 は **0.6023 → 0.6121 → 0.624 と最終epochでも上昇中＝未収束**。
＝「BC は無理」ではなく「**BC に投資していなかった**」の可能性。GPU は遊休、教師コーパスは既存。

出典: disc728071（純模倣 ~21k試合で 900-1000 到達・「policy network のスケール」がレバー）、
disc717697（表現が最重要）。提出サイズ上限 197.7 MiB（disc727565）。

## Stage 1: 多層・大規模化への配線（完了）

既存パイプラインは **1層・d128 専用にハードコード**されていた。アーキ族は変えず、その固定を外した。

- `exp041_pilotnet/pretrain.py`: `MyModel(d, 2, d*2, 1, 1)` と固定だったのを
  `--heads / --enc-layers / --dec-layers / --d-ff` で可変化。`--cosine`（長時間学習用のLR減衰）を追加。
  **`arch.json` を results/<tag>/ に書き出す**（ヘッド数は重み形状から復元できないため、以後の
  npnet/eval/ビルドが構成を推測せず再現できるようにする）。per-epoch の `lr` も history に記録。
- `exp041_pilotnet/npnet.py`（numpy推論）: `encoder.layers.0` 直書きだったのを**層ループ化**。
  `mha()/emb_bag()` から module-level の `D/H/HD` 定数を外し、引数化。`infer_arch()` で
  重み名・形状から層数と d_model を復元（heads は arch.json、無ければ legacy=2）。
- **純Python版（`npmcts_policy.py`）は多層化しない**と決定。理由は速度: 実績 0.13s/act（d128,1+1）に対し
  **2.3s/act は実際にラダーでタイムアウト失敗**した（actTimeout≈1s+overage）。d256/2+2 は純Pythonで
  約1.0s/act 相当＝budget 超過なので、多層を純Pythonで走らせる意味がない。
  → **numpy 経路＝大型ネット / 純Python経路＝旧d128 のフォールバック**という二経路構成にする。

### 検証（Stage 1）
- **回帰**: 既存 pre_grimm10（d128/1+1）で `arch={'d_model':128,'heads':2,'enc_layers':1,'dec_layers':1}` を
  自動復元し、torch vs numpy で **n=500, max|dv|=2.97e-08, max|dp|=5.98e-06, argmax 500/500 → PARITY OK**。
  一般化で既存経路を壊していないことを確認。
- **多層**: ランダム初期化 `MyModel(d=64,h=4,ff=128,enc=2,dec=3)` を export→NpNet で
  **arch 完全一致・n=60, max|dp|=2.76e-07, argmax 60/60 → PARITY OK**。多層・非legacyヘッドで動作確認。

## Stage 2: 本気の学習（進行中）

`config.yaml` / `run.sh` に構成を固定（seed 42, opp-drop 0.5=出荷条件, batch 128, cosine LR）。
コーパスは exp080 の 10日分をそのまま再利用（**1,083,793 決定 / 12,235 試合**）＝データ作成コスト0。
検証は **game 単位 held-out**（`is_val(wid, GID)`、train から除外）＝リーク無しを確認済み。

- **A: d_model=256 / heads=4 / enc2+dec2 = 27.2M params**（baseline は 12.8M）、24 epoch、~305秒/epoch。

### A（warmup なし）= 最適化不全で打ち切り
```
ep0 acc 0.4095 / ep1 0.4945 / ep2 0.5856 / ep3 0.5825 / ep4 0.5870
```
ep2 で **0.586 に頭打ちし停滞**、baseline の 0.624 に届かず。ep0 が baseline(0.6023) より大幅に低く
loss も高い（0.1853 vs 0.1667）＝同一データ・同一LRで容量を増やしただけでここまで劣るのは
**容量の問題ではなく最適化不全**（post-norm transformer を warmup 無しで深くした時の既知の挙動）。
→ 打ち切って warmup を導入。

### warmup 導入 = 診断が的中（A2）
`pretrain.py` に `--warmup-steps`（linear ramp）を追加し、**スケジューラを per-epoch から per-batch に
変更**（warmup はステップ粒度が必要なため `train_epoch(sched=...)` で毎バッチ step）。

| | epoch 0 の acc |
|---|---|
| A（warmup なし） | 0.4095 |
| **A2（warmup 4000 step）** | **0.5740** |

**同一アーキ・同一データで +0.165**。原因が最適化だったことが確定した。
（A2 は再起動後も seed 42 で ep0=0.5740 を正確に再現＝決定的で再現性あり）

### だが warmup だけでは足りず、真因は「ピーク LR が高すぎる」（A2 → 探査 → A3）
A2 は ep0 0.5740 → **ep1 0.4896 → ep2 0.5222 と振動**。warmup は ep0 の途中で終わるので、
ep1 以降は**ピーク LR 3e-4 で走る**＝そこが不安定という切り分けになる。
2時間の賭けを繰り返さず、**3 epoch の短い LR 探査**（warmup 後は定数 LR にして「そのピークが安定か」を
直接見る設計）に切り替え、`--clip`（grad-norm クリップ）も追加した。

**探査結果（lr=1e-4 + clip 1.0 + warmup 4000, 定数）**
```
        probe(1e-4)   baseline d128(3epで終了)   A(3e-4,wuなし)   A2(3e-4,wu)
ep0     0.6077        0.6023                     0.4095           0.5740
ep1     0.6255        0.6121                     0.4945           0.4896
ep2     0.6241        0.6240                     0.5856           0.5222
```
**ep1 で baseline の最終値(0.6240)を超え、以後安定**。原因の切り分けが完了した:
- 3e-4 は公式サンプルの値で **d128/1+1 には適切だが d256/2+2 には高すぎる**
- warmup 単独では不十分（ピーク LR 自体が問題）、**warmup + LR 1e-4 + grad clip** で健全化

### A3 = 本番運転（実行中）
上記レシピ + cosine 減衰 24 epoch（探査は定数 LR だったので、減衰分の伸びしろが残っている）。
~307秒/epoch ＝ 合計約2時間。`run.sh A3` にレシピを固定記録。

**A3 結果（24 epoch 完走, 約2時間）**
```
ep0 0.6053 → ep3 0.6438 → ep8 0.6678 → ep13 0.6808 → ep18 0.6888 → ep23 0.6895
最後の6 epoch が 0.688-0.689 で平坦 ＝ 収束済み
```
| | 旧 baseline d128（7分） | **A3 (d256/2+2, 27.2M)** | 差 |
|---|---|---|---|
| top-1 (oracle-free) | 0.6238 | **0.6892** | **+0.065** |
| vs Alakazam (mixed_ex1) | 0.6267 | **0.7059** | **+0.079** |

ラダーでの最大の穴だった **Alakazam 戦の忠実度が最も伸びた**（+0.079、教師例も最多の相手）。

## Stage 3: オフライン足切り = 3ゲートすべて合格

| ゲート | 基準 | 結果 | |
|---|---|---|---|
| 1. 忠実度 top-1 | ≥0.68 | **0.6892** | 合格 |
| 2. 対旧ネット自己ミラー | 明確に勝ち越し, n≥200 | **0.665**（133-66-1, **z=+4.67**, 0err） | 合格 |
| 3. per-act 時間 | ≤0.3s | **max 0.01s**（numpy経路） | 合格 |

ゲート2 が実質的な判定（ラベルノイズ上限の影響を受けない）。z=+4.67 は偶然では説明できない差。

## Stage 4: 二経路ビルド（`build_dual.py`）

`npmcts_policy.py`（純Python net + 特徴量 + agent + クラッシュ安全）に **numpy 高速経路を追記**する方式。
`MODEL` は `.forward()` を持つ単一オブジェクトなので、numpy 版に差し替えるだけで全経路が切り替わる。

- **fast**: numpy + `cg/npnet.npz`（A3, 100.6MB）＋ `cg/arch.json`（ヘッド数は重み形状から復元不能なため必須）
- **fallback**: numpy か npz が使えなければ、**既存の純Python d128 net**（`cg/weights_pure.pkl`）のまま
- import 時に例外を握りつぶす設計＝**numpy が無くても絶対にクラッシュしない**
- 重みは `cg/` 内（配送実績のある経路）、**`__file__` は一切使わない**（v015 の NameError 事故の教訓）

### 検証（Stage 4）
- ビルド: **148.5MB**（上限 197.7MB 内）、top-level = `[deck.csv, main.py]`、必要ファイル全て存在
- parity: A3 の torch vs numpy で **n=500, max|dp|=1.02e-05, argmax 500/500 → PARITY OK**
- 速度: numpy **2.16 ms/decision**（旧 d128 numpy 0.58ms、旧 d128 純Python 130ms）
- **`sandbox_replica.py`（出荷tarのみ・bare exec・`__file__`なしの実harness再現）**:
  - numpy あり → 169手完走、**max_act 0.01s**、total 0.8s
  - **numpy を意図的に遮断** → 110手完走、**max_act 0.19s**（純Python d128 に自動縮退）、クラッシュなし
  → **両経路とも予算内で動作**することを実証。保険が機能する。
- 標準ガントレット・スモーク: **60試合 / 3マッチアップ 0エラー**
  （勝率 0.30/0.25/0.40 は exp007/exp002 の専用パイロット相手＝旧BCネットが同じ相手に 0.000 を出した
   既知のアーティファクト。この種の局所値はラダーを予測しないと今週2度実証済みなので判定に使わない）

## Stage 5 の前に事故: v042 は Kaggle 側で ERROR（"Validation Episode failed."）

提出 54987775 は検証エピソードで**両エージェントとも ERROR**。診断:

- `ListEpisodes` → validation episode 88157662、`statuses: ["ERROR","ERROR"]`、steps は2つだけ
  （step0 = ACTIVE/初期化、step1 = 両者 ERROR）。
- `remainingOverageTime` が **600 → 598.674**＝**エージェント時間はわずか 1.33秒しか消費していない**。
  ＝**タイムアウトではない**。プロセスが早期に死んでいる（catch できない死に方）。
- 我々の `try/except` で numpy import を包んだ設計は**効かなかった**。例外なら握りつぶせるが、
  ネイティブ拡張の abort / OOM kill / 展開失敗はプロセスごと落ちるので握りつぶせない。

**同じエラー文言・同じ失敗クラスが v015（2026-07-09）で既に起きている**。当時の結論は
`npmcts_policy.py` と `export_pure.py` のヘッダに残っている:
> 「cg が numpy に依存しているから sandbox にも numpy がある」という仮定は一度も検証されておらず、
> この crash によって反証された。

つまり **v042 は 2026-07-09 に一度潰した地雷を踏み直した**。当時 `__file__` 問題と numpy を同時に
直したため numpy 単独の有罪は確定していなかったが、`__file__` を完全に排した v042 が同じ文言で
落ちたことで、**残る差分（numpy 経路 / 148.5MB という桁違いのサイズ）が原因**に絞られた。
どちらが真因でも対処は同じ＝**numpy を使わない・サイズを実績範囲に戻す**。

- 実害: **なし**。ERROR 提出は eligible に入らない（LB の LastSubmissionDate は 07-25 08:31 = v041 のまま、
  スコア 840.5 / #626/5718、silver cut 916.9）。**枠は {v040 koff, v041 alakazam} のまま無傷**。

## 対照実験（完了）: 伸びは「容量」か「学習量」か — そして出荷可能性の壁
d128/1+1 を**同じ良いレシピ（lr1e-4+clip+warmup+cosine, 24ep）**で再学習（`sc083_d128ctl`, 完走）。

| | 旧 d128（7分） | **d128ctl（2時間）** | A3 d256/2+2（2時間） |
|---|---|---|---|
| top-1 (oracle-free) | 0.6238 | **0.6577** | **0.6892** |
| vs Alakazam | 0.6267 | **0.6745** | **0.7059** |
| 対旧ネット n=200（ゲート2） | — | **0.525 (z=+0.71) FLAT** | 0.665 (z=+4.67) PASS |

**切り分けの結論**: 学習量は top-1 を +0.034 上げるが、**強さには変換されなかった**（0.525＝互角）。
A3 の +0.065 のうち、実際に打ち筋の強さになったのは**容量側**。
＝「純Python の小型モデルで同じ性能」という都合の良い出口は**存在しない**（誠実なネガティブ）。

### これが出荷を塞ぐ
- **出せる構成（numpy不要の d128）はゲート2 に落ちる**（0.525）。事前登録した撤退条件に該当。
  旧 d128 級は実ラダー 0.43 で、koff/alakazam の 0.54 に遠く及ばなかった実績もある。
- **ゲート2 を通る構成（A3 d256/2+2）は出荷できない**。純Python 換算の推定は
  numpy 比（d128 0.58ms → A3 2.16ms ＝ 3.7倍）から **約 0.7s/act**＝ゲート3（≤0.3s）超過、
  かつ v015 が 2.3s/act で実際にタイムアウト失敗した領域に近い。重みも純Python形式で ~110MB。
- ⇒ **ゲート的には出荷根拠が無い**。ビルド自体は `build_pure/`（47.9MB, numpy完全排除,
  torch↔出荷純Python パリティ n=300 argmax 300/300, max|dp|=1.24e-05,
  スモーク120試合0err, sandbox replica 144手 max_act 0.29s）として成立している。

**ユーザー判断: ゲート2 不合格を承知でラダー実測する**として v043 を提出（sub 54988388, **COMPLETE**）。
検証エピソードを通過＝**v042 の原因が numpy 経路/巨大アーティファクトだったことが実地で確認された**
（コードは `npmcts_policy.py` バイト同一、差分は numpy の有無とサイズのみ）。v040 koff(845.4) を退出。

**判定基準（事前登録・変更しない）**: 実ラダー ~40試合の勝率が **koff/alakazam の 0.54 を超えること**。
超えなければ即座に実績ビルドへ復帰し、学習レーンを誠実なネガティブとして再度閉じる。
旧 d128 級は実ラダー 0.43 だった＝**事前確率は低い**ことを明記しておく。

## exp083b: 出口を広げてから容量を増やす（sc083_distil / sc083_deep）

### 純Python 推論の高速化（item 3）— これが全ての前提だった
GPU でどれだけ良いネットを作っても、**出口が 0.29s/act で埋まっていれば出せない**。
プロファイルすると `_linear` が **全体の88%**（7.48s / 8.51s）。フラット配列を Python レベルで
1要素ずつ添字アクセスしていたのが原因。**重み行列をロード時に出力行ごとの `array.array` に分割**し、
内積を `sum(map(mul, x, row))`（C 側で完結）に変更。

| | 変更前 | 変更後 |
|---|---|---|
| ms/decision | 126.2 | **68.9** |
| max_act | 0.29s | **0.15s** |

併せて **N層・任意ヘッド数に一般化**（層数=重み名、d_model/d_ff=形状から復元。ヘッド数だけは
復元不能なので `arch.json` 必須）。検証: 既存 d128/1+1 回帰 **300/300**（値も変更前と同一）、
ランダム `d64/h4/enc2+dec3` を実出荷パイプライン経由で **60/60**。

### 蒸留（item 1）— ラベルを柔らかくすると、同じ容量でも強くなる
`d128ctl` の失敗は「人間ラベルの top-1 を当てる能力」は伸びたが打ち筋にならなかったこと。
ハードラベルは**選ばれた1手しか教えない**が、教師は**残り全候補の良し悪し**も教える。
`--teacher/--distill` を追加し、A3 の出力ベクトルを重み0.7でターゲットに混ぜた。

### 層で容量を増やす（sc083_deep）— サイズと計算量の非対称性を突く
**サイズは埋め込み（vocab × d_model）が支配し、計算量は層が支配する**。よって d_model=128 を
据え置いて enc2+dec2 にすれば、**ファイルは ~51MB の実績圏のまま**容量だけ増やせる。
計算量2倍は item 3 の貯金で吸収。**これは item 3 が終わるまで実行不可能な構成だった**。

### 結果

| | top-1 | vs Alakazam | ゲート2 |
|---|---|---|---|
| 旧 d128（7分） | 0.6238 | 0.6267 | — |
| d128ctl（ハードラベル・2h）＝**v043** | 0.6577 | 0.6745 | vs旧 0.525 (z=+0.71) |
| distil（d128 1+1） | 0.6820 | 0.6985 | vs d128ctl **0.573 (z=+2.90)** |
| **deep（d128 h4 2+2）** | **0.6967** | **0.7129** | vs distil 0.545 (z=+1.80) |
| A3 教師（d256 2+2、**出荷不能**） | 0.6892 | 0.7059 | vs旧 0.665 (z=+4.67) |

**deep vs d128ctl（＝ラダー上の v043）: 0.635 (254-141-5, z=+5.40, n=400)**。
3つの改善（学習レシピ・蒸留・層追加）が各々小さく積み上がった結果で、**どれ1つでも届かなかった**。
deep は top-1 でも教師 A3 を超えた（生徒が教師を超えるのは蒸留では珍しくない＝ソフトターゲットの正則化効果）。

### ビルド検証と**ゲート3 の明示的な改訂**
- 48.9MB / パリティ **300/300**(max|dp|=2.49e-06) / arch.json 同梱（heads=4）
- **`arch.json` 未同梱は静かな誤動作**: ヘッド数は形状から復元できないので、無ければ純Python 側は
  既定の2ヘッドで動き、**クラッシュもエラーも出さずに別の関数になる**。
  `build_np_submission.py` に同梱と一致 assert を追加した。
- **max_act = 0.35s ＝ 事前登録した ≤0.3s を超過**。

**ゲート3 を「max_act ≤0.5s かつ 1試合合計 ≤60秒」に改訂した**（ユーザー承認済み）。
理由は「超えたから緩める」ではなく、**0.3s が実績2点（0.13s 通過 / 2.3s 失敗）から置いた代理値に
すぎず、直接の制約を測り直したから**:
- 1試合 **28.5秒（両席）＝1席 ~14秒**、overage bank **600秒**に対し消費 2.4%
- 試合上限は10分。桁が違う
- コード内蔵の安全弁は **1.5秒/手 × 3回でネットを自動切断**＝0.35s はその1/4
- 実運用中の v043 は max_act 0.29s でタイムアウトゼロ

## exp083c: 「模倣が見ている情報」そのものを疑う（v044 提出）

ユーザーの2つの指摘が起点。どちらも当たっていて、片方は想定より深刻だった。

### 指摘1: ±1 回帰は過信ではないか（引いた札からしか選べない以上、観測手＝最善手ではない）
正しい。ただし**負例を完全にマスクすると退化する**（対比が消え、全候補 +1 が最適解になり argmax が無意味になる）。
成立する形として3つ実装（`--policy-loss {huber,margin,ce}`）:
- **margin**（採用）: 観測手より**上に位置する候補だけ**を罰す。下にあるものは損失ゼロ＝「判断できないものは
  supervise しない」を、対比を保ったまま表現できる。ユーザー案に最も忠実。
- **ce**: softmax 交差エントロピー + label smoothing。対比は正規化で作る。
- 方向の妥当性は蒸留で既に実証済みだった（負例を柔らかくする操作＝distil が d128ctl に 0.573, z=+2.90）。

### 指摘2: 実際にポケカで参照する情報を漏れなく入れているか
**入っていなかった。しかも最も基本的なものが。** エンジンが観測として出しているのに**使用回数ゼロ**:

| 区分 | 欠落していたフィールド |
|---|---|
| ターン内資源 | `supporterPlayed` `energyAttached` `stadiumPlayed` `retreated` `turnActionCount` |
| ポケモン | `maxHp`（KO距離）`appearThisTurn`（進化可否）`preEvolution` `energies`（型） |
| プレイヤー | `benchMax` 表向きサイド |
| 文脈 | `looking`（サーチ・公開中に見えているカード） |

**気づきにくかった理由**: 合法性は候補リストが保証している（サポート使用済みなら選択肢が来ない）ので、
**不正手という形では絶対に表面化しない**。飢えていたのは評価と手順構築だけ。
→ ENC_V3 として追加（既存 word 0-24 の**並び順は不変**、各ブロック末尾に追記＋word 25 追加なので
`OPPDECK_WORD=22` のオラクル・ドロップアウト機構はそのまま生存）。
検証: torch側と出荷純Python側を**実リプレイ312局面**で index/value/offset 完全一致、26 words、31,823次元。

### 発見: 価値ヘッドは「工夫の余地」でなく**動いていなかった**
出荷ネットの価値出力を実局面300で測定: **min 0.99986 / max 1.00000 / stdev 0.00002 ＝ 定数**。
原因は `scan_teachers.py` の「we imitate winning play」＝**勝った試合だけを集めていた**こと。
outcome ラベルが全レコード +1 なので、価値ヘッドは「常に1を出せ」と学習していた。

**これが過去の重要なネガティブを無効化する**: 学習レーンを閉じた根拠の1つ
「MCTS レバー = 0.300 (z=-3.10)＝探索は逆効果、価値関数が中盤を読めない」は、
**価値関数が定数だった条件下**の測定だった。定数を評価関数にした探索はノイズを撒くだけ。

### B: コーパス再生成（ENC_V3 × 負け試合込み）
```
1,949,324 レコード / 22,830 試合   (旧 1,083,793 / 12,235 = 1.8倍)
outcome +1 55% / -1 45%   ← 価値ヘッドに初めて信号
```
外部報告（disc728071「純模倣 ~21k試合で 900-1000」）の水準を初めて超えた。
**勝ち試合縛りで半分のデータを捨てていた**ことになる。

### A: 教師 sc083_v3t（d256/2+2, ENC_V3 + 負け込み + margin, 16ep）
```
top-1 0.7587  Alakazam 0.7731
価値ヘッド AUC:  序盤 0.631 → 終盤 0.942   （旧: 全部 null＝定義不能）
```
AUC の形も筋が通っている（序盤はほぼ予測不能、終盤に向け単調上昇）。

### MCTS レバー再測定 = **完全に反転**
```
exp080（価値ヘッド定数）: 0.300 (z=-3.10)  「探索は逆効果」
exp083c（価値ヘッド実働）: 0.733 (z=+3.61)  44-16-0, 0err
```
振れ幅 +0.43。**今日ネットワーク側で稼いだどの改善より大きい**。ただし探索量への依存が急:
```
sc=4  0.517 (z=+0.26) FLAT   /   sc=8  0.583 (z=+1.29)   /   sc=16  0.733 (z=+3.61)
```
安価な sc4 では消える。sc16 は純Python で ~1.2s/act ＝改訂後の予算(≤0.5s)にも収まらない。
**活かすなら「終盤限定 sc16」**——価値ヘッドの AUC が序盤 0.63/終盤 0.94 なので、
評価が信頼できる終盤に探索を集中させるのが精度・計算量の両面で合理的。未実装。

### 生徒 sc083_v3s = 出荷版（v044）
**deep と完全に同一構成**（d128/h4/enc2+dec2）にして、差分を exp083c の3修正だけに限定。
```
ゲート2: v3s vs deep (n=400) = 0.603  (241-149-10, z=+4.10, 0err)
参考:    deep vs v043のネット  = 0.635  (z=+5.40)
```
**比較器を書き直す必要があった**（`eval_cross.py`）: 2つのネットは重みだけでなく**入力特徴量が違う**
（26 words/vocab34000 vs 25/24000）。train_mcts は特徴量バージョンをモジュール大域で持つので、
通常の対戦だと片方に相手の特徴量を食わせるか、そもそもロードで落ちる。各エージェントが
自分のバージョンを持ち回る形にした。

### 出荷検証（v044）
- 53.6MB / パリティ **300/300**(max|dp|=1.58e-06) / **max_act 0.20s**・243手・合計35.1秒
- スモーク120試合 **0エラー**（wr 0.450/0.500/0.550。専用パイロット相手なので判定には使わないが、
  順序が対戦ゲートと一致＝deep 0.250/0.050/0.550 > v043 0.100/0.100/0.250）
- **`verify_ship_enc.py`**: 出荷tarのみ・bare exec・`__file__`なし・リポジトリ外 cwd から
  **ENC_V3=1 / NUM_WORDS_ENCODER=26 / heads=4 を解決し、実局面で26 words を生成**することを実証。
  v1 にフォールバックすると**静かに別の関数になりエラーは出ない**ので専用チェックにした。
- **`arch.json` 同梱＋一致 assert を追加**（`build_np_submission.py`）: ヘッド数と特徴量バージョンは
  重み形状から復元不能。今日 v042 で踏んだのと同じ「エラーを出さない失敗」クラス。

### 提出（v044, ユーザー承認済み）
eligible = **{v043 545.8, v044}**。**v041 alakazam 773.6 を退出**（816.8→802→792→773.6 と低下継続中だった）。
枠の算術上、新規提出は常に最古＝alakazam を押し出すため、v043 の死に枠は温存される。
**段階戦略**: ~40試合で v044 を評価 → 良ければ2枚目を出して v043 を押し出し独立2ドロー、
悪ければ実績ビルドへ復帰。**v044 が 773.6 を超えるまでは名目上の後退**であり、それが引き受けたリスク。

### 次の一手（枠コスト0の候補）
A3 の利得が「埋め込み幅」か「層の深さ」かをもう一段分ける。**計算コストは層に、サイズは埋め込みに**
偏っている（27.2M のうち大半は vocab×d の EmbeddingBag、行列演算は層側）ので、
**d128 のまま enc2+dec2** なら**ファイルは ~51MB の実績圏のまま**で容量だけ増やせる。
純Python の実行コストは約2倍（max_act 0.19s → ~0.4s）でゲート3 ぎりぎり＝
純Python NpNet の多層化と高速化がセットで必要。これが通れば出荷可能な形で A3 の利得に近づく。

## 判定原則（重要）
**local eval はラダーを予測しない**（pub1034 BC ゲート・dragapult で2度実証、[[local-not-ladder]]）。
本実験の offline 指標（top-1、対旧ネット自己ミラー）は**「枠を使う価値があるか」の足切り専用**であり、
出荷根拠にしない。出荷判定は**実ラダー ~40試合の勝率が koff/alakazam の 0.54 を超えるか**のみ。

## 既知のリスク / 未確定
- **sandbox の numpy 可用性**: `npnet.py` は「numpy あり torch なし（exp032/033で確立）」と記述、
  disc716302 の traceback（`/kaggle_simulations/agent/main.py`）に
  `/usr/local/lib/python3.11/dist-packages/transformers/` と numpy が見えるため可用性は高いと判断。
  ただし**未確定**なので、numpy 失敗時は旧 d128 純Python net にフォールバックする設計にする（クラッシュ不可）。
- **提出サイズ**: A は 27.2M params ≈ 109MB(float32 npz) + フォールバック 47MB ≈ 156MB で 197.7MB 以内。
  **B（d384/4+4）は推定 ~48M params ≈ 192MB でフォールバック同梱だと上限超過**の見込み。
  → B に進む場合は float16 保存や フォールバック省略などの対処が要る（要実測）。

---

# exp083e (07-29): 探索を net に還元する — **不合格、レーンを閉じた**

## 仮説
実測で「同一 net の探索(sc16) vs 生argmax = 0.825 (z=+5.81)」＝**探索は現 net より強い方策**。
AlphaZero の1反復として、その探索出力を教師ラベルにして net 自体を引き上げる。

## 実施
- `selfplay_gen.py`: `train_mcts.selfplay()` の LearnSample を pretrain の12タプルへ変換し、
  **13番目に soft 方策（子ごとの優位度 [-1,1]）** を追加。`make_batch` は13番目があればそれを、
  無ければ従来の one-hot を使う（後方互換）。
- 生成: sc083_v3s の自己対戦 800試合 / sc16 → 162,333決定（2.75時間、490MB）。
- `sc083_sp`: v3s から resume、self-play のみ、lr 2e-5、4 epoch、`--policy-loss huber`。

## 結果（事前登録ゲート）
```
sc083_sp vs sc083_v3s   raw argmax 同士 / 同一デッキ / 先後入替 / n=400
78-320-2   勝率 0.195   z=-12.20   errors=0        → NEGATIVE
```
**改善ではなく崩壊。** 事前登録の撤退条件（1反復で効果が見えなければ閉じる）に従い閉じる。

## 原因（特定済み）: 教師信号が平坦すぎた
```
候補間の方策スコア差 (max-min):  中央値 0.030 / 平均 0.105
差 < 0.05 の局面:               83.6%          (多候補局面 n=18,566, 平均6.11候補)
```
BC 側の方策ヘッドは **margin 損失で ±1 スケールに分離**して学習されている。そこへ
**幅 0.03 の目標**を Huber 回帰でかぶせたため、方策ヘッドがほぼ定数に潰れ、argmax が実質ランダム化した。

**sc16 では優位度の解像度が足りない**（平均6候補に16シミュレーション＝1候補あたり2〜3訪問）。
AlphaZero が数百〜数千シミュレーションを使う理由がここ。再挑戦には sc64〜128 が必要で、
生成コストが 2.75h → 11〜22h。締切（8/16）に対する時間対効果が悪いので閉じる。

## 再発した罠: オフライン指標は上がっていた
```
探索一致率（self-play held-out top-1）: 0.6472 -> 0.7247  （上昇）
実際の対戦勝率:                          0.195            （崩壊）
```
目標が平坦なら argmax は偶然一致しやすく、順位情報を持たない。
**「top-1 が上がっても強さは上がらない」（exp064, d128ctl）の、より極端な再現。**
[[local-not-ladder]] と同じ判定原則で救われた形＝**head-to-head を必ず通す運用が機能した**。

## 副次的に確認できた良い事実
```
価値目標 (TD blend): mean -0.020  stdev 0.687  |v|>0.99 が 12.4%
```
自己対戦の価値ラベルに勝敗の分布がちゃんと入っている＝**7/26 の「価値ヘッドが定数」修正は生きている**。

## 計測上の欠陥（修正済み、記録として）
`to_record(s, done)` の `done` はバッチ単位（10刻み）だったため gid が全て10の倍数になり、
`is_val` の `%20` 分割が **50/50 に退化**していた（学習に使えるのが半分）。
`spfix_v3s_w0.pkl` として gid を振り直し（gid//10）、val 5% に是正してから本番学習した。

# exp083f (07-29): 探索予算の上限は妥当か — **FLAT、レーンを閉じた**

v045 の実ラダー消費は中央値 293.6s / 600s ＝**予算の半分が未使用**。探索曲線も
sc4 0.517 / sc8 0.583 / sc16 0.733 と単調で、頭打ち位置が未測定だった。
`sc_ladder.py`（同一 net・同一デッキ・先後入替、探索回数だけが差）で測定:
```
MCTS(sc32) vs MCTS(sc16)   36-44-0   sc32勝率 0.450   z=-0.89   n=80   errors=0
```
**sc16 は既にプラトー上。** 予算の余りは探索回数では換金できない。v045 の SEARCH_COUNT=16 は
結果的に正しい設定だった。上限引き上げ案は棄却。

# exp083g (07-29/30): 教師データの「量」と「質」を分けて測る

## 背景
v046（15日コーパス＝量）を提出した時点で、silver cut までの差は 34.0点。
v045 は 907.2 → 893.8 → 881.8 と試合数の増加に伴って**下降**しており、
「あと8.8点」は上振れのピークだった＝真の実力の上積みが要る。

## G-1: 量（V15S）— **PASS、v046 として出荷**
```
sc083_v15s vs sc083_v3s   生argmax同士 n=400   243-156-1   0.608  z=+4.30  0err
                          探索あり同士 n=80     47-33-0     0.588  z=+1.57  0err
```
コーパス 10日 → 15日（1,949,324 → 3,545,132決定、41,188試合、**46%がミラー**）。
アーキ・出荷コード・探索制御・ステップ数は固定＝**教師データだけが変数**。

## G-2: 質（E11S）— **FLAT、単独では置き換えにならない**
教師の足切りを LB 1000点（118チーム）→ **1100点（上位20チーム）** に変更。
855,593決定 / 9,333試合（15日版の1/4）、59%がミラー。
```
sc083_e11s vs sc083_v15s   生argmax同士 n=400   191-209-0   0.477  z=-0.90  0err
```
**質だけでは量に勝てない。** ただし**1/4のデータ量で互角**＝質は量をある程度代替している。
→ 排他ではないので、カリキュラム（広く学ぶ → elite で寄せる）を V15E として別に測る。

## 学習曲線から見つけた2つの欠陥（どちらも修正済み）
```
V15S (8ep)   0.7196 0.7324 0.7380 0.7420 0.7448 0.7464 0.7476 0.7501
V3S (16ep)   0.7107 0.7294 0.7364 0.7396 0.7446 0.7462 0.7479 0.7497 ... 0.7577
```
**(1) V15S は学習が半分で切れていた。** 最初の8エポックが V3S とほぼ重なり、V3S はその先も伸びる。
つまり **V15S は半分の学習量で V3S に 0.608 で勝っていた**。→ V15S2（ウォームリスタート、
コサイン第2周期・ピークLR半分・warmupなし）で残り半分を与える。ゼロから16エポックより安い。
```
E11T  0.7324 0.7446 0.7495 0.7526 0.7530 0.7548 0.7548 0.7538 0.7538 0.7553(ep9)
      0.7531 0.7532 0.7531 0.7521 0.7513 0.7494 0.7500 0.7484 0.7469 ... 0.7507(ep23)
```
**(2) elite 教師は ep9 をピークに過学習していた。** 855k に24エポックは多すぎ。
最終チェックポイントを教師にすると**過学習した先生から蒸留**することになるので ep9 に差し替え、
学生も10エポックに短縮。なお**学生側は過学習しない**（10エポックで 0.7197 → 0.7584 と上昇継続）＝
蒸留が正則化として効いている。

## G-3: 学習量（V15S2, ウォームリスタート）— **FLAT**
V15S の曲線が V3S の前半と重なり、V3S はその先も伸びていたので「V15S は半分で切れている」と判断し、
コサイン第2周期（ピークLR半分・warmupなし・seed変更）で8エポック追加。
```
V15S2  0.7457 0.7470 0.7478 0.7490 0.7501 0.7506 0.7513 0.7535   (top-1 +0.0034)
sc083_v15s2 vs sc083_v15s   生argmax同士 n=400   209-188-3   0.522  z=+0.90  0err  → FLAT
```
**top-1 は上がったが強さに変換されない。** 学習量はもう効かない＝V15S は既に飽和していた。

## ここまでで見えた法則（重要・Strategy レポート用）

**変換されたもの（＝ネットが「何を見るか / 何から学ぶか」を変えた施策）**
| 施策 | 変えたもの | head-to-head |
|---|---|---|
| exp083b DEEP | 層を増やす＋蒸留目標 | 0.635 (z=+5.40) |
| exp083c V3S | ENC_V3 特徴量＋負け試合＋margin損失 | 0.603 (z=+4.10) |
| exp083d 探索 | 推論時に先読みを足す | 0.825 (z=+5.81) |
| exp083g V15S | 教師コーパス 10日→15日 | 0.608 (z=+4.30) |

**変換されなかったもの（＝同じものを「もっと」やった施策）**
| 施策 | 変えたもの | head-to-head | オフライン指標 |
|---|---|---|---|
| exp083 d128ctl | 学習時間を伸ばす | 0.525 FLAT | top-1 **+0.034（上昇）** |
| exp083e SP | 自己対戦の探索ラベル(sc16) | **0.195 崩壊** | 探索一致率 **+0.078（上昇）** |
| exp083f sc32 | 探索回数を倍 | 0.450 FLAT | — |
| exp083g E11S | 教師の足切りを厳しく | 0.477 FLAT | — |
| exp083g V15S2 | 学習量を倍 | 0.522 FLAT | top-1 **+0.0034（上昇）** |

**結論**: このネットは**情報の入り口（特徴量・データの新しさ・推論時の先読み）でしか伸びない**。
最適化の量（epoch・容量・探索回数・教師の厳選）は既に飽和している。
そして**負けた5件のうち3件でオフライン指標は上昇していた**＝top-1 で判断していたら3回とも誤って出荷していた。
head-to-head n=400 を必須にする運用が、この局面での唯一の防波堤になっている。

## G-4: カリキュラム（V15E: 広く学ぶ → elite で寄せる）— **FLAT**
V15S2 から elite コーパスで低LR微調整（3 epoch, lr 2e-5, 教師 e11t ep9）。
```
V15E (elite val)  0.7644 0.7647 0.7660     ← 全モデル中で最高
E11S (同じval)     0.7197 ... 0.7584
sc083_v15e vs sc083_v15s   生argmax同士 n=400   190-209-1   0.475  z=-1.00  0err  → FLAT
```
**上位20チームの判断への一致度は最高なのに、実対戦では負け越す。**
top-1 に騙されかけたのはこれで4回目（d128ctl / SP / V15S2 / V15E）。

## この round の結論
「改善施策」3本（質・学習量・カリキュラム）は**すべて FLAT**。出荷しない。
v046 = sc083_v15s が引き続き我々の最良ネット。
残る有望なレバーは**法則どおり「情報の入り口」側のみ**:
 (a) さらに新しい教師データ（07-28以降のエピソードを追加）＝v046 を通した唯一の勝ち筋の継続
 (b) 特徴量の追加（ENC_V4）＝ENC_V3 が 0.603 を生んだのと同じ種類のレバー
 (c) 探索アルゴリズム自体の改良（回数は sc16 で飽和済みだが、決定化や価値の使い方は未着手）

---

# exp083i (07-30): ゲートの設計ミスを特定 — ミラー限定評価が v046 の劣化を隠していた

## 症状
```
              ラダー   Alakazam    lucario_ex   ミラー     全体勝率
v045 (v3s)    886.9   0.70 n=40   0.62 n=8    0.39 n=41   0.537 (136試合)
v046 (v15s)   740.4   0.50 n=12   0.27 n=15   0.33 n=6    0.537 ( 67試合)
```
**勝率は同一 0.537。差は「誰に勝っているか」**＝v046 はより弱い相手に同じ勝率しか出せていない。

## 原因: gate.py / eval_cross.py は両席とも Grimmsnarl の自己ミラーだった
つまり n=400 の 0.608 は「**ミラーが強くなったか**」しか測っていない。
15日コーパスは **46%がミラー**なので、ミラーに特化してフィールドを失う劣化は
**この設計では原理的に観測不能**だった。

## `gate_field.py` を追加して既知の答えで検証
相手＝実ラダーの遭遇シェア（v045 n=136 実測）で重み付けした6アーキタイプ。
lucario_ex は ladder pool の約87%が単一パイロット lucario_v2 なのでローカル代役が忠実。
```
              ミラー  Alakazam  Crustle  lucario_v2  Dragapult  Spidops   weighted
v15s (v046)   0.990   0.800     0.630    0.340       0.930      0.990     0.829
v3s  (v045)   1.000   0.830     0.660    0.450       0.850      1.000     0.854
delta         -0.01   -0.03     -0.03    -0.11       +0.08      -0.01     -0.025
```
**向きがラダーと一致（v3s > v15s）。穴の場所も一致（lucario 0.34 ↔ ラダー 0.27）。**

## 結論（判定原則の更新）
これまで「local eval はラダーを予測しない」と記録してきたが、正確には
**「ミラー限定の local eval はラダーを予測しない」**。予測しなかったのではなく、
**測っていた対象が違った**。以後 head-to-head ミラーゲートは
「ミラーが劣化していないことの確認」に格下げし、**出荷判定は gate_field.py の重み付き合計**とする。

# exp083j (07-31): ENC_V4 は判定できず／教師データに Mega Lucario ex がいなかった

## ENC_V4 の結果（基準未達、提出せず）
```
生argmax  n=100   V4S 0.812  v3s 0.834   差 -0.021
探索あり  n= 40   V4S 0.877  v3s 0.861   差 +0.016   （基準 +0.03）
```
符号が入れ替わった。ENC_V4 が足したのは obs.select と remainDamageCounter / remainEnergyCost で、
**局面評価＝価値ヘッドに効く情報**。価値ヘッドを使うのは探索なので、
生argmax（方策ヘッドのみ）で測ると効果が出にくいのは筋が通る。
ただし n=40 のばらつきは ±0.03〜0.04 なので +0.016 は利得の証拠にならない。**判定できなかった**。

### ゲート自体の測定精度（記録）
同じ v3s を3回測って 0.854 / 0.834 / 0.861。
→ **v046 のような大きな劣化（lucario -0.11）は確実に捉えるが、±0.02 の優劣は判定できない。**
±0.02 を分けたいなら n=300 以上が要る（探索ありだと1回あたり15時間規模）。

## 教師データの相手構成を数えた（今日の本題）
16日コーパス 4,062,285決定:
```
mixed_ex3 (Grimmsnarl/オーロンゲ, ミラー)  1,963,245  48%
mixed_ex1 (Alakazam/フーディン)              932,787  23%
...
lucario_ex (Mega Lucario ex/メガルカリオex)    3,619  0.09%   ← 一番負けている相手
```
実ラダーでは lucario_ex は相手の 6〜22%。**戦い方をほとんど教わっていない相手に一番負けている。**

日ごと（各日100エピソードのサンプル、min_score=1000 の教師席のみ）:
```
07-03〜07-12   lucario 3.4〜20%   ミラー  0〜35%
07-13〜07-28   lucario 0%         ミラー  9〜67%（07-27 は 66.7%）
```
disc729926 の「上位帯で 28% → 0.03% に絶滅」と一致。ただし**我々のいる帯には今も残っている**。

## 対応: 28日コーパス（実行中）
07-01〜07-12 を足すと (a) 足りない lucario 戦が入る (b) ミラー比率が下がる。
v046 が壊れた原因がミラー偏重（46%）だったので、両方とも直したい向き。
**ENC_V3 で作る**——v3s との違いをデータだけにして、日数の効果を単独で見るため。

# exp083k (07-31): 28日コーパス — FLAT、ただし狙った1マスは両測定で改善

## コーパス
```
4,904,410決定 / 59,694試合 / 28日 (07-01..07-28)  13.7GB   ENC_V3
                    16日版          28日版
mixed_ex3(ミラー)    48%      →      44%
lucario_ex          0.09%     →     0.57%  (3,619 -> 28,071件, 7.8倍)
```

## ゲート結果（gate_field.py, 生argmax, 対 v3s）
```
n=100   D28S 0.846   v3s 0.793   delta +0.053   (1.3σ)   → PASS判定だがノイズ
n=300   D28S 0.841   v3s 0.825   delta +0.016   (0.7σ)   → FLAT
```
**n=100 の PASS はノイズだった。** v3s の測定値は 0.793 / 0.825 / 0.834 / 0.854 / 0.861 と散らばる。

### 測定誤差の実測と計算（今後の設計に使う）
重み付き合計の標準誤差:
```
n=100:  片側 0.028   差 0.040
n=300:  片側 0.016   差 0.023
```
v3s を5回測った実測レンジ 0.793〜0.861（幅0.068）は n=100 の se=0.028 と整合する。
**+0.03 という当初の PASS 基準は、この誤差を知る前に決めたもので、n=100 では 0.75σ にしかならない。**
以後は n=300 を既定とし、+0.05 以上（2σ超）を PASS とする。

### 狙った1マス（lucario_v2）は両方の測定で改善
```
n=100   v3s 0.330 -> D28S 0.390  (+0.06)
n=300   v3s 0.430 -> D28S 0.467  (+0.04)
```
方向は一貫。ただし全体を動かすほどではない。0.57% では足りない。

## 結論
出荷判定としては FLAT。ただし枠の状況が変わっており、押し出す対象は最弱の v046(741.9)。
D28S は v3s と統計的に区別できない＝v046 より確実に強いので、
**枠に入れること自体は LB を下げるリスクがない**（v047 860.7 が残るため）。
判断はユーザーに委ねる。
