# exp085 — GBDT オプション・ランカー

## 仮説

transformer BC は 16件の誠実なネガティブを積んで止まっている。ローカルで区別できず、
ラダーでは退行する（28日コーパスが約70点、gate_field は FLAT と判定）。

公開ノートブック tetsutani（`grimmsnarl-ex-damage-transfer-control`, 08-04 時点 864.4）は
まったく別の学習器で同程度に到達している。中核は**勾配ブースティング決定木のアンサンブル**で、
選択肢1つずつを平坦な特徴量の行にして採点し、上位 k 個を取る。約1,300木・180KB・探索なし・
純Python推論。手書きガードはその上に多段の上書きとして乗る。

仮説: この規模のデータでは、系列モデルより**選択肢ランキング**の方が分散が小さく、
特徴量に名前があるので誤りを特定して直せる。

**模倣したのは問題の形（選択肢を明示的な特徴量でランク付けする）だけ。**
コードも重みも取り込んでいない。特徴量は engine API から自前で設計した。

## 構成

```
feats.py            309特徴量／選択肢1行
build_rows.py       教師リプレイ → 行 + 0/1ラベル、qid でクエリを区切る
train_gbdt.py       LambdaRank、SelectContext を5系統に分けて別モデル
export_pure.py      LightGBM → 純Python木テーブル（numpy不要）
parity_pure.py      LightGBM と純Python版の一致検証
gbdt_policy.py      エージェント本体。フォールバック回数を数える
build_submission.py tar 組み立て（main.py に feats/policy を連結）＋実artifactスモーク
eval_h2h.py         harness.run_gauntlet での対戦。--artifact で出荷tarを直接測る
diag_errors.py      誤りを context / OptionType / カード別に分解
```

## 結果

### v3（採用）— デッキ完全一致・28日・600,055決定

学習（held-out）:

| family | 学習query | 検証query | top-k | top-1 |
|---|---:|---:|---:|---:|
| main | 243,974 | 27,109 | 0.6976 | 0.6976 |
| c7 | 80,216 | 8,867 | 0.6154 | 0.6139 |
| mid | 172,072 | 19,116 | 0.8217 | 0.8324 |
| low | 15,286 | 1,674 | 0.7569 | 0.8058 |
| easy | 28,499 | 3,166 | 0.9163 | 0.8995 |

全体 exact-set top-k **0.7382**（59,932決定）。純Python版とのパリティは完全一致
（最大差 0.000e+00、top-k 750/750）。木 625本・50MB→**2.57MB**。

対戦（**出荷tar** を `harness.run_gauntlet` に通す、n=200、席交替、フォールバック0）:

| 相手 | GBDT v3 | 現行 v3s+MCTS |
|---|---:|---:|
| **現行ビルド（v3s+MCTS sc16）** | **0.670** (134-64-2, z+4.81) | — |
| Alakazam | 0.880 (z+10.75) | 0.810 |
| dragapult | 0.695 (z+5.52) | 0.890 |
| lucario_v2 | 0.640 (z+3.96) | 0.533 |
| Archaludon | 0.630 (z+3.68) | 0.710 |
| Crustle | 0.615 (z+3.25) | 0.485 |

最大手番時間 0.098〜0.164秒（現行 MCTS 版の約1/10）。エラー0件。

## 誠実なネガティブと、その原因

### v2 — アーキタイプ・ラベルで作ったコーパスは別デッキだった

40万決定を 07-01〜07-11 から作ったところ、教師再現は 0.684 → **0.798** に上がったのに
強さは落ちた。原因はコーパスで、**我々の60枚は 07-17 以前のメタに存在しない**。

| 期間 | 教師の60枚が我々と完全一致する割合 |
|---|---:|
| 07-01〜07-09 | 0.00 |
| 07-10〜07-16 | 0.00〜0.14 |
| 07-17〜07-23 | 0.33〜0.57 |
| 07-24〜07-28 | 0.55〜0.73 |
| 全体 | 0.233 |

`mixed_ex3` というアーキタイプ・ラベルは同じでも中身は別構築。**持っていないカードのプレイを
忠実に覚えた**ため、忠実度が上がって強さが下がった。v3 は 60枚完全一致だけを使い、
14,392シートを除外している。

**transformer 側の28日コーパス（v048、ラダーで約70点の劣化、ローカルゲートは FLAT）も
同じ作り方**なので、同一原因である可能性が高い。

### 静かに劣化する不具合を3件（すべてエラー0件で通過していた）

| 箇所 | 症状 | 見え方 |
|---|---|---|
| `load_ref.py` | 参照エージェントの lazy import 失敗 | 196決定中115で適当な合法手。3-117 で「我々が圧勝」に見えた |
| `feats.py` | `train_mcts` import 失敗（サンドボックスに無い） | 全カードIDが 0。出荷tarが 0.150/0.000/0.100 |
| `build_submission.py` | 連結時に `feats.option_rows` の参照が解決不能 | **全判断がフォールバック**。0勝25敗×3、`err0=0`、スモークは「OK」 |

いずれも例外を握り潰して**合法だが無意味な手**を返す形。クラッシュしないので気づけない。
対策として `gbdt_policy` にフォールバック回数のカウンタを入れ、スモークで 0 を要求するようにした。

### 自前の評価ループが壊れていた

`eval_gbdt.py`（当日書いた自前ループ）は `harness._validate_selection` を通さず、
デッキと席を手で割り当てていた。同じモデルで自前ループ 0.480、`run_gauntlet` 0.120。
**今日の GBDT 対戦数値のうち、`eval_gbdt.py` 由来のものはすべて破棄した。**

物差しの検証を `--defer all`（ミラー戦）で済ませたのが誤り。**両席が同じデッキなので
デッキと席の取り違えは原理的に見えない。**7月末にも同型の失敗（ミラー限定ゲート）をしている。

以後の対戦はすべて `harness.run_gauntlet` ＋ `--artifact`（出荷tar直接）で測る。

### 記事から取り込んだ設計（invenglobal 24133, DeNA / Pokémon TCG Pocket）

| 記事 | 我々の対応 |
|---|---|
| 行動を原子単位に分解して行動空間の爆発を避ける | 選択肢の独立採点＋上位k。現行 MCTS の組み合わせ列挙とは逆 |
| 単一行動局面は学習データから除外 | 実装済み（v3 で 53,678決定を除外） |
| 誤用はカードへの罰でなく**補助特徴量**で直す（正用率 96.2%） | 下記 |

`diag_errors.py` が v1 で一貫した偏りを検出した:
ABILITY 603 / EVOLVE 404 を過剰に上位へ、ATTACK 220 / RETREAT 101 / END 57 を過剰に落とす
＝「何かをやり続けて、攻撃・撤退・ターン終了をしない」。
特徴量に**技のダメージ値が一切なかった**（`opt_attack_id` から暗記するしかない）。
`opt_attack_damage / energy / lethal / margin` と局面側13個を追加（292→309）。
ATTACK の扱いは落とし220:上げ109（2:1）→ 落とし524:上げ586（ほぼ均衡）に変化した。

## 出典

- 公開ノートブック `tetsutani/grimmsnarl-ex-damage-transfer-control`（構造の観察のみ。
  ライセンス上ローカル解析専用、`ref_tetsutani*/` は gitignore、再配布しない）
- disc732905 "How consistent is imitation learning in this setting?"（同一エージェントで
  50位〜150位に振れるという他参加者の報告。ラダーのノイズが我々の見積もりと整合）
- invenglobal 24133（DeNA / Pokémon TCG Pocket の RL AI）

---

## v4 / 探索（8/6 夜、進行中）

### 特徴量 309 → 318（誤り診断ループ 2周目）

v3 の held-out 誤り 0.262（MAIN 52.3% + TO_HAND 21.7% で全体の74%）。
v2 で足した攻撃ダメージでも解消しない偏りが残っていた:

| OptionType | 誤って上位に | 誤って落とす | 比 |
|---|---:|---:|---|
| ABILITY | 2413 | 1655 | 1.46 : 1 |
| EVOLVE | 966 | 652 | 1.48 : 1 |
| ATTACK | 351 | 678 | 1 : 1.93 |
| RETREAT | 99 | 261 | 1 : 2.64 |
| END | 36 | 160 | 1 : 4.44 |

誤って上位に入れたカードの1位は **Munkidori（ダメカン移動の特性）で 2,888件**。

原因: **モデルは「このターンに既に何回使ったか」を見られない。** prev1..3 は直前3手までなので、
ターンの8手目を決める時点で「もう2回動かした」と「まだ何もしていない」が区別できない。

追加:
`turn_actions_so_far` / `turn_abilities_used` / `turn_plays_used` / `turn_attaches_used` /
`turn_evolves_used` / `opt_src_acted_this_turn` / `opt_same_action_this_turn` /
`opt_counters_to_ko` / `opt_tgt_hp_ratio`

履歴の持ち方を `(turn, semantic)` に変更し、窓を 3 → 40 に拡張（1ターンが十数手になるため）。

**作業中に見つけた2件:**

1. ダメカン系の対象解決が `inPlayArea` 経由だけだった。DAMAGE_COUNTER 文脈は
   `area` + `playerIndex` で対象を指すため、**追加した特徴量が全て 0** だった。
   `semantic()` と同じフォールバックを入れて修正（785行で発火するようになった）。
2. `remainDamageCounter` はリプレイ観測では**常に 0**。これを使う「カウンターが足りるか」の
   フラグは一度も発火しないので、木が自力で作れない比率（対象の残HP割合）に差し替えた。

### 探索: 検証付き上書き（`search.py`）

MCTS を全面移植せず、**v013 の upside-only gate / v014 の turn-beam と同じ設計**を採った。

```
1. GBDT で全選択肢を採点し、上位M個の候補手を作る（部分集合の全列挙はしない）
2. 各候補を determinize したエンジンで適用し、自ターンが終わるまで GBDT で貪欲に打ち切る
3. 到達局面を価値モデルで評価する
4. 方策の第1候補を MARGIN 以上上回るときだけ上書きする
```

`determinize` は exp040 の実装を使う（`npmcts_policy.py` に純Python移植版が既にあり、
現行提出で動いているので出荷経路も確保済み）。

選択の理由: MCTS は実装面積が大きく、**この実験だけで静かに劣化する不具合が3件**出ている。
方策を主役に置き先読みを確認にだけ使う設計なら、壊れても方策の答えに戻る。

動作確認: **26ミリ秒/決定**（1試合600秒の予算に対して桁違いに軽い）。

### 価値モデル（`build_value.py` / `train_value.py`）

探索には局面評価が要るが、既存コーパスは**勝者のみ**で学習信号が無い。両席を取る
コーパスを新設した。ロールアウトではなく学習モデルを選んだのは速度: ロールアウトは
1回0.2秒で1手25回程度しか回せないが、決定木なら数マイクロ秒で3桁多く評価できる。

**学習分布の偏り（記録）:**
- 負けの行は**ミラー戦に偏る**。両席が我々のデッキを持つのはミラーだけなので、
  「我々のデッキが Alakazam に負ける」局面はラダーより大幅に少ない。
- ラベルは最終結果なので、**強いパイロットが打ち切った互角局面も 1** になる。
  探索は兄弟局面の順序さえ合えば良いので単調変形には耐えるが、絶対値は勝率ではない。

### 測定の方針

**変更は一度に1つずつ測る。** v2 でコーパスと特徴量を同時に変えて切り分け不能にした反省。

1. v4 学習 → 出荷tarで対 v3（318特徴量の効果を単独で測る）
2. 価値モデル学習 → 同一モデルで探索あり vs なし
3. 両方が勝ち越したら提出候補。押し出すのは v051（860.3）なので v052 の収束を見てから

---

## 探索 — 誠実なネガティブ（8/7、レーンを閉じた）

`search.py`（検証付き上書き）＋ 価値モデル（80万行、AUC 0.9086）で A/B を2回:

| 上書き閾値 | n | 探索なし | 探索あり | 差 | 上書き率 |
|---|---:|---:|---:|---:|---:|
| 0.02 | 60 | 0.567 | 0.467 | -0.100 (z -1.10) | 10.8% |
| 0.12 | 120 | 0.608 | 0.542 | -0.067 (z -1.03) | 5.1% |

個々は有意でないが**符号が一致**しており、閾値を上げて上書きを半減させても改善しない
＝上書きが多すぎるのではなく、**上書きの選び方が良くない**。

原因の見立ては価値モデル。AUC 0.9086 は全局面の数字で終盤が簡単なだけで、探索が要求する
**同一ターン内の兄弟局面**の順序は捉えられていない。学習分布の偏り（負けの行がミラー戦に
偏る、互角局面も勝ちラベル）もこの用途では効く。正しく直すには価値の学習方法を変える必要が
あり（局面の優劣を直接学ぶ、自己対戦で作る）、残り9日では合わないと判断。

**速度は問題なかった**（軽い設定で平均22ms/決定、1試合1.9秒）。最初に報告した「38秒/手」は
重い設定（determinize 2回・24手先読み）での単発値。

### 符号バグ（記録）

初回測定は **0.015（3-197）**。原因は `search.py` に自分で書いたコメントの通りの反転を
実装していなかったこと。`_greedy_finish` は自ターン終了時点で止まるので評価対象は相手の
手番であり、`base_state` は手番側視点なので、探索は**相手の勝率を最大化する手**を選んでいた。
`value_of(oc, our_index)` に署名を変え、手番が自分でなければ 1-v を返すよう修正。

この実験で4件目の「静かに劣化する不具合」だが、前3件と違い**大きく壊れたので即座に分かった**。
危険なのはもっともらしい数字を出す壊れ方の方。

## 容量スイープ（8/7）— 特徴量より先に試すべきだった

`sweep_main.py`。main は全決定の45%・誤りの55%を占め、v4 では 600ラウンド中 271本で
早期停止していた。

| 葉 | lr | min_leaf | top-k | 木 |
|---:|---:|---:|---:|---:|
| 63 | 0.080 | 40 | 0.6962 | 271 |
| 127 | 0.060 | 40 | 0.7071 | 288 |
| 255 | 0.050 | 60 | 0.7096 | 162 |
| **511** | **0.040** | **100** | **0.7143** | 204 |
| 1023 | 0.035 | 150 | 0.7128 | 229 |
| 2047 | 0.030 | 200 | 0.7152 | 147 |

511 で頭打ち（以降 ±0.002 は誤差）。2047 はノード数3倍で得るものがないため 511 を採用。
**28万クエリに対して葉63枚が単純に手狭だった。**

## v4b（採用候補）— コーパスも特徴量も v4 と同一、容量だけ変更

| | v3 | v4 | v4b |
|---|---:|---:|---:|
| main | 0.6976 | 0.6962 | 0.7114 |
| c7 | 0.6154 | 0.6374 | 0.6437 |
| mid | 0.8217 | 0.8528 | 0.8575 |
| low | 0.7569 | 0.7923 | 0.7908 |
| easy | 0.9163 | 0.9231 | 0.9245 |
| **全体 top-k** | 0.7382 | 0.7522 | **0.7616** |
| lucario_v2 | 0.640 | 0.575 | **0.680** |
| Crustle | 0.615 | **0.710** | 0.705 |
| dragapult | 0.695 | **0.810** | 0.755 |
| Archaludon | 0.630 | **0.685** | 0.655 |
| 対現行 | 0.670 | 0.650 | 測定中 |

モデル 2.99MB → 16.69MB、tar 1.5MB → 6.5MB。最大手番は 0.23〜0.38秒で v4（0.43〜0.55秒）
より**速い**（木の本数が減ったため）。

## 出荷検証が黙って飛んでいた（8/7、修正済み）

`run_v4b.sh` 実行中に `parity_pure.py` が `FileNotFoundError` で落ちたが、スクリプトが
止まらず**パリティ未検証のまま tar がビルドされた**。原因はモデルのタグで行データを探す作りで、
v4b は v4 の行で学習しているため `rows_v4b.pkl` が存在しないこと。

修正: `--rows` を分離し、**比較件数が 0 なら例外**を投げる。ファイルが無いことが「検証OK」に
化けない。修正後の v4b は差 0.000・600/600 で通過。

## 残る課題

Munkidori（ダメカン移動の特性）を過剰に上位へ置く偏り 3,137件が、ターン単位の履歴を足しても
解消していない（ABILITY の 上げ:落とし は v3 1.46:1 → v4 1.43:1 でほぼ不変）。
**特性の効果が盤面をどう変えるかがモデルに見えていない。** 技のダメージ値は `all_attack()` から
機械的に取れたが、特性の効果はテキストなので同じ手が使えず、手作業になる。
